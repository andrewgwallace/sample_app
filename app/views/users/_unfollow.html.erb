<!-- Manually created. -->
<!-- Please see NOTES.txt for Listing 12.22. A form for unfollowing a user. -->

<!-- Listing 12.34. A form for following a user using Ajax. -->
<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),

				html: { method: :delete },
				remote: true) do |f| %>
	<%= f.submit "Unfollow", class: "btn" %>
<% end %>
